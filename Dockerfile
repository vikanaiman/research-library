# Используем легкий Python 3.12
FROM python:3.12-slim

# Рабочая папка внутри контейнера
WORKDIR /app

# Копируем все файлы сайта внутрь
COPY . .

# Открываем порт 8888 (как в твоем скрипте serve.py)
EXPOSE 8888

# Запускаем скрипт
CMD ["python", "serve.py"]
