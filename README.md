# ML Team Dashboard

Статическая информационная страница с конфигурируемым контентом.

## 🚀 Запуск на lerning-сервере

1. **Скопируйте файлы на сервер:**

   ```bash
   scp -r ./* lerning:~/your-folder/
   ```

2. **Запустите сервер:**

   ```bash
   ssh lerning
   cd ~/your-folder
   python serve.py
   ```

3. **Откройте в браузере:**
   ```
   http://lerning:8888
   ```

## ⚙️ Настройка контента

Все данные хранятся в `config.json`. Просто отредактируйте файл и обновите страницу.

### Структура конфига:

```json
{
  "site": {
    "title": "Заголовок сайта",
    "subtitle": "Подзаголовок"
  },
  "hero": {
    "heading": "Главный заголовок",
    "description": "Описание под заголовком"
  },
  "cards": [
    {
      "icon": "🔬",
      "title": "Название карточки",
      "description": "Описание",
      "link": "#section-id"
    }
  ],
  "sections": [
    // ... секции с разным контентом
  ]
}
```

### Типы секций:

- **Со статусами** (эксперименты): `items` с `status` и `statusType` (success/progress/pending)
- **С метриками**: `items` с `name` и `value`
- **Со ссылками**: `links` с `text` и `url`
- **Команда**: `members` с `name`, `role`, `contact`

## 📁 Структура проекта

```
lerning-page/
├── index.html      # Главная страница
├── styles.css      # Стили
├── app.js          # Логика загрузки конфига
├── config.json     # ← Редактируйте этот файл!
├── serve.py        # Python-сервер
└── README.md       # Документация
```

## 💡 Советы

- После изменения `config.json` просто обновите страницу (F5)
- Сервер слушает на `0.0.0.0`, поэтому доступен из сети
- Можно запустить в фоне: `nohup python serve.py &`
