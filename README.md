# Research Library

Интерактивная библиотека научных исследований, статей и видео с мульти-дисциплинарной навигацией и фильтрацией по тегам.

## 🚀 Запуск проекта

### Локальный запуск

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Запустите сервер разработки:**
   ```bash
   npm start
   ```

3. **Откройте в браузере:**
   ```
   http://localhost:3000
   ```

### Запуск через Python-сервер

1. **Запустите сервер:**
   ```bash
   python serve.py
   ```

2. **Откройте в браузере:**
   ```
   http://localhost:8888
   ```

## ⚙️ Настройка контента

Все данные хранятся в `config.json`. Отредактируйте файл и обновите страницу.

### Структура конфига

```json
{
  "site": {
    "title": "Название библиотеки",
    "subtitle": "Описание библиотеки"
  },
  "disciplines": [
    {
      "id": "discipline-id",
      "name": "Название дисциплины",
      "subtitle": "Описание дисциплины",
      "icon": "🎯",
      "pdfBasePath": "./papers/",
      "articles": [
        {
          "id": 1,
          "pdf": "filename.pdf",
          "title": "Название статьи",
          "description": "Описание исследования",
          "tags": {
            "companies": ["Компания"],
            "researchType": ["Тип исследования"],
            "problem": ["Проблема"],
            "application": ["Область применения"],
            "technologies": ["Технология"],
            "additional": ["Доп. тег"]
          }
        }
      ],
      "externalLinks": [
        {
          "id": 5001,
          "type": "article",
          "url": "https://example.com/article",
          "title": "Название статьи",
          "description": "Описание",
          "tags": { "..." : "..." }
        },
        {
          "id": 6001,
          "type": "pdf",
          "pdf": "local-file.pdf",
          "title": "PDF-обзор",
          "description": "Описание",
          "tags": { "..." : "..." }
        },
        {
          "id": 7001,
          "type": "video",
          "url": "https://youtube.com/...",
          "title": "Видео",
          "description": "Описание",
          "tags": { "..." : "..." }
        }
      ]
    }
  ]
}
```

### Типы внешних материалов

| Тип | Иконка | Действие |
|-----|--------|----------|
| `article` | 🟢 ссылка | Открывает URL в новой вкладке |
| `video` | 🔴 play | Открывает URL в новой вкладке |
| `pdf` | 🟠 документ | Открывает PDF из `papers/` в модалке |

### Категории тегов

- **companies** — Компании
- **researchType** — Тип исследования (Техническое, Прикладное и т.д.)
- **problem** — Проблемы, которые решает исследование
- **application** — Области применения
- **technologies** — Используемые технологии
- **additional** — Дополнительные теги

## 📁 Структура проекта

```
research-library/
├── index.html          # Главная страница
├── styles.css          # Стили
├── app.js              # Логика: навигация, фильтрация, модалки
├── config.json         # ← Данные: дисциплины, статьи, ссылки
├── serve.py            # Python-сервер для локального запуска
├── papers/             # PDF-файлы статей и обзоров
└── README.md           # Документация
```

## 💡 Возможности

- **Мульти-дисциплинарная навигация** — Переключение между дисциплинами через навбар
- **Фильтрация по тегам** — Мульти-фильтр с AND-логикой и динамическими счётчиками
- **Внешние материалы** — Секция со статьями, видео и PDF-обзорами
- **Якорная навигация** — Быстрый переход к внешним материалам с счётчиком
- **Просмотр PDF** — Встроенный модальный просмотрщик
- **Расширяемые описания** — Развёртывание полных описаний
- **Кнопка «Наверх»** — Плавная прокрутка к облаку тегов
- **Адаптивный дизайн** — Удобный просмотр на любых устройствах

## 🔧 Советы

- После изменения `config.json` просто обновите страницу (F5)
- PDF-файлы должны находиться в `papers/`
- Для внешних ссылок типа `article` и `video` используйте поле `url`
- Для типа `pdf` используйте поле `pdf` (имя файла в `papers/`)
- Теги работают одинаково для статей и внешних материалов