# Research Library

Интерактивная библиотека научных исследований с фильтрацией по тегам.

## 🚀 Запуск проекта

### Локальный запуск

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Запустите сервер разработки:**
   ```bash
   npm start
   ```

3. **Откройте в браузере:**
   ```
   http://localhost:3000
   ```

### Запуск через Python-сервер

1. **Запустите сервер:**
   ```bash
   python serve.py
   ```

2. **Откройте в браузере:**
   ```
   http://localhost:8888
   ```

## ⚙️ Настройка контента

Все данные о статьях хранятся в `config.json`. Просто отредактируйте файл и обновите страницу.

### Структура конфига:

```json
{
  "site": {
    "title": "Название библиотеки",
    "subtitle": "Описание библиотеки"
  },
  "pdfBasePath": "./papers/",
  "articles": [
    {
      "id": 1,
      "pdf": "filename.pdf",
      "title": "Название статьи",
      "description": "Описание исследования",
      "tags": {
        "companies": ["Компания1", "Компания2"],
        "researchType": ["Тип исследования"],
        "problem": ["Проблема1", "Проблема2"],
        "application": ["Область применения"],
        "technologies": ["Технология1", "Технология2"],
        "additional": ["Дополнительные теги"]
      }
    }
  ]
}
```

### Доступные категории тегов:

- **companies** - Компании, связанные с исследованием
- **researchType** - Тип исследования (Техническое, Прикладное и т.д.)
- **problem** - Проблемы, которые решает исследование
- **application** - Области применения
- **technologies** - Используемые технологии
- **additional** - Дополнительные теги

## 📁 Структура проекта

```
research-library/
├── index.html          # Главная страница
├── styles.css          # Стили
├── app.js              # Логика загрузки и фильтрации
├── config.json         # ← Редактируйте этот файл!
├── serve.py            # Python-сервер для локального запуска
├── papers/             # PDF-файлы статей
└── README.md           # Документация
```

## 💡 Возможности

- **Фильтрация по тегам** - Быстрый поиск статей по категориям
- **Просмотр PDF** - Встроенный просмотрщик PDF-файлов
- **Расширяемые описания** - Развертывание полных описаний статей
- **Адаптивный дизайн** - Удобный просмотр на любых устройствах

## 🔧 Советы

- После изменения `config.json` просто обновите страницу (F5)
- PDF-файлы должны находиться в папке, указанной в `pdfBasePath`
- Для добавления новой статьи достаточно добавить её в массив `articles` в `config.json`